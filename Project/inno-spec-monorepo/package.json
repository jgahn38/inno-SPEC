{
  "name": "@inno-spec-monorepo/source",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "npx nx run designer-app:dev",
    "build": "npx nx run-many --target=build --all",
    "build:designer-app": "npx nx run designer-app:build",
    "build:shared": "npx nx run shared:build",
    "build:database-lib": "npx nx run database-lib:build"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "empty": "0.10.1"
  },
  "devDependencies": {
    "@nx/eslint-plugin-nx": "^16.0.0-beta.1",
    "@nx/js": "^21.5.1",
    "@nx/vite": "^21.5.1",
    "@nx/workspace": "21.5.1",
    "nx": "21.5.1"
  }
}
