{
  "name": "@inno-spec-monorepo/source",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "nx run main-app:dev",
    "build": "nx run-many --target=build --all",
    "build:main-app": "nx run main-app:build",
    "build:shared": "nx run shared:build",
    "build:database-lib": "nx run database-lib:build"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "empty": "0.10.1"
  },
  "devDependencies": {
    "@nx/eslint-plugin-nx": "^16.0.0-beta.1",
    "@nx/js": "^21.5.1",
    "@nx/vite": "^21.5.1",
    "@nx/workspace": "21.5.1",
    "nx": "21.5.1"
  }
}
